import Title from "../title/Title";

const Header = (props) => {
    return (
        <>
            <h1>Header</h1>
            <Title titleInclude="I'm header comp"/>

        </>
    );
}

export default Header;